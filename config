
to add a user to sudoers and no password required
sudo visudo
user ALL=(ALL)    NOPASSWD: ALL

ansible.cfg  ----- #ask_sudo_pass

ssh-keygen
ssh-copy-id user@IP


yum list installed | grep requiredPackage     
man ansible-doc

ansible hostname -i hosts -m ping     ----to give hostname explicitly through a different hosts file residing in same(in this case)
or different directory

ansible all --list-hostsin 
cat /etc/ansible/hosts
